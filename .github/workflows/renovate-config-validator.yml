# Never make this workflow a required status check. It only runs when
# .github/renovate.jsonc changes; on any other PR GitHub keeps a skipped required
# check "Pending" forever, so every dependency PR would be blocked from merging.
name: Renovate Config Validator
on:
  pull_request:
    branches:
      - main
    paths:
      - .github/renovate.jsonc
  push:
    branches:
      - main
    paths:
      - .github/renovate.jsonc
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # The same action MintMaker runs on its own repository. It checks syntax
      # and schema only: a rule that matches no file or package still passes.
      - uses: konflux-ci/renovate-config-validator-action@772ce0f2d37cd13ad80408a4902c7a17f3191e3a # main
        with:
          config_file: .github/renovate.jsonc
          strict: true
